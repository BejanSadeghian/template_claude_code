# My always-follow instructions

Free-form custom rules Claude reads every session and always follows. Project-wide
and committed — for personal/uncommitted rules use `CLAUDE.local.md` (gitignored)
or `~/.claude/CLAUDE.md` (applies to all your repos).

Add yours below. Examples:

<!--
- TODOs live in `docs/todo.md`, newest at the top.
- Never edit files under `vendor/` or `generated/`.
- Prefer functional React components; no class components.
- When you finish a feature, add a one-line entry to `CHANGELOG.md`.
-->
