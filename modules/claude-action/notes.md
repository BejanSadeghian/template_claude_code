Claude Code action installed. Setup:
- Run `/install-github-app` from a Claude Code terminal (repo admin) to install the app + secret, OR
- Add repo secret ANTHROPIC_API_KEY manually (or use Bedrock/Vertex/OIDC for keyless).
- Mention @claude in an issue or PR comment to trigger it.
